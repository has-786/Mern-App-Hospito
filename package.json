{
  "name": "myfirstreactnpx",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "google-map-react": "^1.1.5",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-geolocated": "^3.0.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
     "start": "node app/server.js",
      "heroku-postbuild": "cd myfirstreactnpx && npm install && npm run build"
   
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
        "node": "8.11.3"
    },
  "proxy": "http://localhost:8080"
}
